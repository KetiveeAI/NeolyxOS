// Dummy heap_alloc and heap_free implementations (backed up from heap.c)
void* heap_alloc(size_t size) {
    // TODO: Replace with real allocator
    return 0;
}

void heap_free(void* ptr) {
    // TODO: Replace with real free logic
} 